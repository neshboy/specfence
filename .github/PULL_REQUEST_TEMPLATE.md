## What does this change

<!-- One or two sentences on what this PR does and why. -->

## Checklist

- [ ] `npm test` passes locally
- [ ] `npm run lint` and `npm run typecheck` pass locally
- [ ] Added or updated tests for the behavior change
- [ ] Updated docs under `docs/` if user-facing behavior changed
- [ ] If this changes `packages/action`, `dist/index.js` was rebuilt (`npm run build` at the repo root) and committed

## Scope

<!-- If this repo is itself gated by SpecFence, note which .specfence/scope.yaml entry this PR falls under. -->
